name: run
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: run
        uses: actions/checkout@v2
      - name: ok
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'
      - name: script
        run: python main.py
